<MessungPage>:
    orientation: 'vertical'
    Label:
        text: "Messwerte"
        font_size: '28sp'
        bold: True
        halign: 'center'
        valign: 'middle'
    GridLayout:
        cols: 2
        spacing: '30dp'
        size_hint_y: None
        height: self.minimum_height
        Label:
            text: "pH-Wert:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
        Label:
            text: root.ph_value
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
        Label:
            text: "Pool:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
            valign: 'middle'
        Label:
            text: f"{root.pool_temp:.1f} °C"
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
            valign: 'middle'
        Label:
            text: "Poolhaus IN:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
            valign: 'middle'
        Label:
            text: f"{root.poolhaus_in_temp:.1f} °C"
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
            valign: 'middle'
        Label:
            text: "Poolhaus OUT:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
            valign: 'middle'
        Label:
            text: f"{root.poolhaus_out_temp:.1f} °C"
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
            valign: 'middle'
        Label:
            text: "Wärmepumpe OUT:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
            valign: 'middle'
        Label:
            text: f"{root.waermepumpe_out_temp:.1f} °C"
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
            valign: 'middle'
        Label:
            text: "Poolhaus:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
            valign: 'middle'
        Label:
            text: f"{root.poolhaus_temp:.1f} °C"
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
            valign: 'middle'
        Label:
            text: "Wärmepumpe:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
            valign: 'middle'
        Label:
            text: f"{root.wp_current_temp:.0f} °C / {root.wp_target_temp:.0f} °C"
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
            valign: 'middle'
        Label:
            text: "TDS:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
        Label:
            text: root.tds_value
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
        Label:
            text: "Leistung Pool:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
            valign: 'middle'
        Label:
            text: root.pool_power
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
            valign: 'middle'
        Label:
            text: "Verbrauch Pool:"
            font_size: '18sp'
            text_size: self.size
            halign: 'right'
            valign: 'middle'
        Label:
            text: f"{root.pool_energy_today} / {root.pool_energy_yesterday}"
            font_size: '18sp'
            text_size: self.size
            halign: 'left'
            valign: 'middle'
        # Weitere Messwerte nach Bedarf
